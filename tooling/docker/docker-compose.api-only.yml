# ===========================================
# LibreDiary Docker Compose - API-Only Override
#
# For deployments where the web SPA is hosted
# separately (e.g. Cloudflare Pages) and nginx
# runs on the host.
#
# Usage:
#   docker compose \
#     -f tooling/docker/docker-compose.production.yml \
#     -f tooling/docker/docker-compose.api-only.yml \
#     up -d postgres server
#
# Developed by Akaal Creatives
# https://www.akaalcreatives.com
# ===========================================

services:
  server:
    ports:
      - "127.0.0.1:3000:3000"
